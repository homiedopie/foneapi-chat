module.exports = ({ location, msg: message, param: field, value, nestedErrors }) => {
    // Build your resulting errors however you want! String, object, whatever - it works!
    return `${message}`
};